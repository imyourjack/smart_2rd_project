<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.BoardMapper">
    
    <select id="loginMember" resultType="userVO">
        select * from user_tbl where user_id=#{user_id} and password=#{password}
    </select>

    <insert id="coffeeContents" >
        insert into reco_tbl(wondu_n, user_id) values(#{wondu_n},#{user_id})
    </insert>
    <select id="wondu_name" resultType="userVO">
        select wondu_n from reco_tbl where user_id=#{user_id}
    </select>

    
</mapper>
